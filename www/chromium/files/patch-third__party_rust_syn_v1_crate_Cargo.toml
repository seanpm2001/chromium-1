--- third_party/rust/syn/v1/crate/Cargo.toml.orig	2022-01-20 10:36:24 UTC
+++ third_party/rust/syn/v1/crate/Cargo.toml
@@ -1,58 +1,35 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2018"
+rust-version = "1.31"
 name = "syn"
-version = "1.0.81" # don't forget to update html_root_url and syn.json
+version = "1.0.81"
 authors = ["David Tolnay <dtolnay@gmail.com>"]
-license = "MIT OR Apache-2.0"
+include = ["/benches/**", "/build.rs", "/Cargo.toml", "/LICENSE-APACHE", "/LICENSE-MIT", "/README.md", "/src/**", "/tests/**"]
 description = "Parser for Rust source code"
-repository = "https://github.com/dtolnay/syn"
 documentation = "https://docs.rs/syn"
-categories = ["development-tools::procedural-macro-helpers"]
 readme = "README.md"
-include = [
-    "/benches/**",
-    "/build.rs",
-    "/Cargo.toml",
-    "/LICENSE-APACHE",
-    "/LICENSE-MIT",
-    "/README.md",
-    "/src/**",
-    "/tests/**",
-]
-edition = "2018"
-rust-version = "1.31"
+categories = ["development-tools::procedural-macro-helpers"]
+license = "MIT OR Apache-2.0"
+repository = "https://github.com/dtolnay/syn"
+[package.metadata.docs.rs]
+all-features = true
+rustdoc-args = ["--cfg", "doc_cfg"]
+targets = ["x86_64-unknown-linux-gnu"]
 
-[features]
-default = ["derive", "parsing", "printing", "clone-impls", "proc-macro"]
-derive = []
-full = []
-parsing = []
-printing = ["quote"]
-visit = []
-visit-mut = []
-fold = []
-clone-impls = []
-extra-traits = []
-proc-macro = ["proc-macro2/proc-macro", "quote/proc-macro"]
-test = ["syn-test-suite/all-features"]
+[package.metadata.playground]
+features = ["full", "visit", "visit-mut", "fold", "extra-traits"]
 
-[dependencies]
-proc-macro2 = { version = "1.0.32", default-features = false }
-quote = { version = "1.0", optional = true, default-features = false }
-unicode-xid = "0.2"
-
-[dev-dependencies]
-anyhow = "1.0"
-flate2 = "1.0"
-insta = "1.0"
-rayon = "1.0"
-ref-cast = "1.0"
-regex = "1.0"
-reqwest = { version = "0.11", features = ["blocking"] }
-syn-test-suite = { version = "0", path = "tests/features" }
-tar = "0.4.16"
-termcolor = "1.0"
-walkdir = "2.1"
-
 [[bench]]
 name = "rust"
 harness = false
@@ -61,14 +38,61 @@ required-features = ["full", "parsing"]
 [[bench]]
 name = "file"
 required-features = ["full", "parsing"]
+[dependencies.proc-macro2]
+version = "1.0.32"
+default-features = false
 
-[package.metadata.docs.rs]
-all-features = true
-targets = ["x86_64-unknown-linux-gnu"]
-rustdoc-args = ["--cfg", "doc_cfg"]
+[dependencies.quote]
+version = "1.0"
+optional = true
+default-features = false
 
-[package.metadata.playground]
-features = ["full", "visit", "visit-mut", "fold", "extra-traits"]
+[dependencies.unicode-xid]
+version = "0.2"
+[dev-dependencies.anyhow]
+version = "1.0"
 
-[workspace]
-members = ["dev", "json", "tests/crates", "tests/features"]
+[dev-dependencies.flate2]
+version = "1.0"
+
+[dev-dependencies.insta]
+version = "1.0"
+
+[dev-dependencies.rayon]
+version = "1.0"
+
+[dev-dependencies.ref-cast]
+version = "1.0"
+
+[dev-dependencies.regex]
+version = "1.0"
+
+[dev-dependencies.reqwest]
+version = "0.11"
+features = ["blocking"]
+
+[dev-dependencies.syn-test-suite]
+version = "0"
+
+[dev-dependencies.tar]
+version = "0.4.16"
+
+[dev-dependencies.termcolor]
+version = "1.0"
+
+[dev-dependencies.walkdir]
+version = "2.1"
+
+[features]
+clone-impls = []
+default = ["derive", "parsing", "printing", "clone-impls", "proc-macro"]
+derive = []
+extra-traits = []
+fold = []
+full = []
+parsing = []
+printing = ["quote"]
+proc-macro = ["proc-macro2/proc-macro", "quote/proc-macro"]
+test = ["syn-test-suite/all-features"]
+visit = []
+visit-mut = []
