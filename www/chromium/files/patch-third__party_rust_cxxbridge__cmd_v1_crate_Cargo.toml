--- third_party/rust/cxxbridge_cmd/v1/crate/Cargo.toml.orig	2022-01-20 10:36:24 UTC
+++ third_party/rust/cxxbridge_cmd/v1/crate/Cargo.toml
@@ -1,27 +1,49 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2018"
+rust-version = "1.48"
 name = "cxxbridge-cmd"
 version = "1.0.56"
 authors = ["David Tolnay <dtolnay@gmail.com>"]
-edition = "2018"
-rust-version = "1.48"
-license = "MIT OR Apache-2.0"
+exclude = ["build.rs"]
 description = "C++ code generator for integrating `cxx` crate into a non-Cargo build."
-repository = "https://github.com/dtolnay/cxx"
 homepage = "https://cxx.rs"
-exclude = ["build.rs"]
 keywords = ["ffi"]
 categories = ["development-tools::ffi"]
+license = "MIT OR Apache-2.0"
+repository = "https://github.com/dtolnay/cxx"
+[package.metadata.docs.rs]
+targets = ["x86_64-unknown-linux-gnu"]
 
 [[bin]]
 name = "cxxbridge"
 path = "src/main.rs"
+[dependencies.clap]
+version = "2.33"
 
-[dependencies]
-clap = "2.33"
-codespan-reporting = "0.11"
-proc-macro2 = { version = "1.0.26", default-features = false, features = ["span-locations"] }
-quote = { version = "1.0", default-features = false }
-syn = { version = "1.0.70", default-features = false, features = ["parsing", "printing", "clone-impls", "full"] }
+[dependencies.codespan-reporting]
+version = "0.11"
 
-[package.metadata.docs.rs]
-targets = ["x86_64-unknown-linux-gnu"]
+[dependencies.proc-macro2]
+version = "1.0.26"
+features = ["span-locations"]
+default-features = false
+
+[dependencies.quote]
+version = "1.0"
+default-features = false
+
+[dependencies.syn]
+version = "1.0.70"
+features = ["parsing", "printing", "clone-impls", "full"]
+default-features = false
